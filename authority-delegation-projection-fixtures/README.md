# Authority Delegation Projection Fixtures

These fixtures project approval authority delegation lifecycle events into a dashboard-ready state.

Run:

```bash
node run-authority-delegation-projection-fixtures.js
```

