{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://controls.local/schema-files/self-improvement-decision.schema.json",
  "title": "Self-Improvement Decision",
  "type": "object",
  "required": [
    "schema_version",
    "type",
    "id",
    "campaign_id",
    "checkpoint_id",
    "mission_id",
    "cycle_number",
    "decision",
    "execution_authorized",
    "promotion_scope",
    "release_authorized",
    "selected_candidate_id",
    "accepted_revision",
    "proof",
    "score",
    "reasons",
    "blocking_codes",
    "next_task_order",
    "human_decision_required",
    "required_human_decision",
    "decided_at"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "string", "enum": ["0.2", "0.3", "0.4"] },
    "type": { "type": "string", "const": "SelfImprovementDecision" },
    "id": { "$ref": "common.schema.json#/$defs/id" },
    "campaign_id": { "$ref": "common.schema.json#/$defs/id" },
    "checkpoint_id": { "$ref": "common.schema.json#/$defs/id" },
    "mission_id": { "$ref": "common.schema.json#/$defs/id" },
    "cycle_number": { "type": "integer", "minimum": 1 },
    "decision": { "type": "string", "enum": ["accept_working_state", "revise_and_retry", "rollback", "escalate", "continue", "complete", "terminate"] },
    "execution_authorized": { "type": "boolean" },
    "promotion_scope": { "type": "string", "enum": ["none", "in_progress_work", "working_state"] },
    "release_authorized": { "type": "boolean", "const": false },
    "selected_candidate_id": { "$ref": "common.schema.json#/$defs/id" },
    "accepted_revision": { "type": "string", "minLength": 1 },
    "proof": {
      "type": "object",
      "required": ["verification_receipt_ids", "parent_decision_id", "approval_consumption_event_id", "repository_manifest_revision", "repository_manifest_sha256"],
      "additionalProperties": false,
      "properties": {
        "verification_receipt_ids": { "type": "array", "uniqueItems": true, "items": { "$ref": "common.schema.json#/$defs/id" } },
        "comparative_evaluation_report_id": { "type": "string", "minLength": 1 },
        "parent_decision_id": { "type": "string", "minLength": 1 },
        "approval_consumption_event_id": { "type": "string", "minLength": 1 },
        "repository_manifest_revision": { "type": "integer", "minimum": 0 },
        "repository_manifest_sha256": { "type": "string", "pattern": "^(none|[a-f0-9]{64})$" },
        "verification_attestation_ids": { "type": "array", "uniqueItems": true, "items": { "$ref": "common.schema.json#/$defs/id" } },
        "verifier_key_ids": { "type": "array", "uniqueItems": true, "items": { "type": "string", "pattern": "^[a-f0-9]{64}$" } },
        "verifier_independence_groups": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "attestation_quorum_satisfied": { "type": "boolean" },
        "comparative_evaluation_attestation_ids": { "type": "array", "uniqueItems": true, "items": { "$ref": "common.schema.json#/$defs/id" } },
        "comparative_verifier_key_ids": { "type": "array", "uniqueItems": true, "items": { "type": "string", "pattern": "^[a-f0-9]{64}$" } },
        "comparative_verifier_independence_groups": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "comparative_attestation_quorum_satisfied": { "type": "boolean" }
      }
    },
    "score": {
      "type": "object",
      "required": ["weighted_before", "weighted_after", "weighted_delta", "minimum_delta", "all_hard_gates_passed"],
      "additionalProperties": false,
      "properties": {
        "weighted_before": { "type": "number", "minimum": 0, "maximum": 1 },
        "weighted_after": { "type": "number", "minimum": 0, "maximum": 1 },
        "weighted_delta": { "type": "number" },
        "minimum_delta": { "type": "number", "minimum": 0, "maximum": 1 },
        "all_hard_gates_passed": { "type": "boolean" }
      }
    },
    "reasons": { "$ref": "common.schema.json#/$defs/string_list" },
    "blocking_codes": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
    "next_task_order": {
      "type": "object",
      "required": ["owner", "task", "purpose", "constraints", "required_evidence", "next_checkpoint_trigger"],
      "additionalProperties": false,
      "properties": {
        "owner": { "$ref": "common.schema.json#/$defs/role_id" },
        "task": { "type": "string", "minLength": 1 },
        "purpose": { "type": "string", "minLength": 1 },
        "constraints": { "$ref": "common.schema.json#/$defs/string_list" },
        "required_evidence": { "$ref": "common.schema.json#/$defs/string_list" },
        "next_checkpoint_trigger": { "type": "string", "enum": ["wave_start", "wave_end", "validation_failure", "quality_regression", "scope_change", "before_completion", "manual"] }
      }
    },
    "human_decision_required": { "type": "boolean" },
    "required_human_decision": { "type": "string", "minLength": 1 },
    "decided_at": { "$ref": "common.schema.json#/$defs/timestamp" }
  }
}
