{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://controls.local/schema-files/verification-attestation.schema.json",
  "title": "Signed Verification Attestation",
  "type": "object",
  "required": ["schema_version", "type", "id", "receipt_id", "receipt_relative_path", "receipt_sha256", "campaign_id", "mission_id", "cycle_number", "candidate_id", "candidate_revision", "repository_binding", "verifier_id", "key_id", "independence_group", "execution_origin", "invocation_id", "envelope", "issued_at", "expires_at", "attestation_sha256"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "string", "const": "0.1" },
    "type": { "type": "string", "const": "VerificationAttestation" },
    "id": { "$ref": "common.schema.json#/$defs/id" },
    "receipt_id": { "$ref": "common.schema.json#/$defs/id" },
    "receipt_relative_path": { "type": "string", "minLength": 1 },
    "receipt_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "campaign_id": { "$ref": "common.schema.json#/$defs/id" },
    "mission_id": { "$ref": "common.schema.json#/$defs/id" },
    "cycle_number": { "type": "integer", "minimum": 1 },
    "candidate_id": { "$ref": "common.schema.json#/$defs/id" },
    "candidate_revision": { "type": "string", "minLength": 1 },
    "repository_binding": {
      "type": "object",
      "required": ["repository_key", "identity_fingerprint"],
      "additionalProperties": false,
      "properties": {
        "repository_key": { "type": "string", "minLength": 1 },
        "identity_fingerprint": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
      }
    },
    "verifier_id": { "$ref": "common.schema.json#/$defs/id" },
    "key_id": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "independence_group": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$" },
    "execution_origin": { "type": "string", "enum": ["local", "remote"] },
    "invocation_id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$" },
    "envelope": {
      "type": "object",
      "required": ["payloadType", "payload", "signatures"],
      "additionalProperties": false,
      "properties": {
        "payloadType": { "type": "string", "const": "application/vnd.in-toto+json" },
        "payload": { "type": "string", "pattern": "^[A-Za-z0-9+/]+={0,2}$" },
        "signatures": {
          "type": "array",
          "minItems": 1,
          "maxItems": 1,
          "items": {
            "type": "object",
            "required": ["keyid", "sig"],
            "additionalProperties": false,
            "properties": {
              "keyid": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
              "sig": { "type": "string", "pattern": "^[A-Za-z0-9+/]+={0,2}$" }
            }
          }
        }
      }
    },
    "issued_at": { "$ref": "common.schema.json#/$defs/timestamp" },
    "expires_at": { "$ref": "common.schema.json#/$defs/timestamp" },
    "attestation_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
  }
}
