interface:
  display_name: "Controls Doctrine Operator"
  short_description: "Run routed, evidence-backed agent missions"
  default_prompt: "Use $controls-doctrine-operator to open this mission through the enforced lifecycle, issue role-bounded context, persist evidence, and close with an AAR and bounded follow-on work."
