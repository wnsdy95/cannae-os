interface:
  display_name: "Controls Doctrine Operator"
  short_description: "Route doctrine and govern bounded adaptive work"
  default_prompt: "Use $controls-doctrine-operator to route this mission and run evidence-backed, authority-bounded improvement checkpoints."
